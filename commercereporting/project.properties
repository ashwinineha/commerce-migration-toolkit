# ---------------------------------------------------------------------------
# Copyright (c) 2019 SAP SE or an SAP affiliate company. All rights reserved.
# ---------------------------------------------------------------------------

# commercereporting.key=value

# Specifies the location of the spring context file putted automatically to the global platform application context.
commercereporting.application-context=commercereporting-spring.xml

log4j2.logger.migrationToolkit.appenderRef.migration.ref=MigrationAppender
log4j2.logger.migrationToolkit.additivity=false

migration.ds.source.db.driver=${db.driver}
migration.ds.source.db.url=${db.url}
migration.ds.source.db.username=${db.username}
migration.ds.source.db.password=${db.password}